# All-in-One Requirements (BRD Module + PDF + DOCX + FastAPI)
# Install this to get everything working:
#   pip install -r requirements-full.txt

# Core BRD Module
psycopg2-binary>=2.9.0
python-dotenv>=0.19.0

# Export Format Support
python-docx>=0.8.11      # DOCX generation with template support
weasyprint>=60.0          # PDF generation with styling
markdown>=3.5.0           # Markdown parsing and HTML conversion

# FastAPI Integration
fastapi>=0.95.0
uvicorn>=0.21.0
pydantic>=1.10.0
python-multipart>=0.0.7
gunicorn>=21.2.0

# Integration Module Dependencies (Gmail, Slack, PDF Parsing)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
slack-sdk>=3.0.0
pymupdf>=1.22.0

# Noise Filter Module Dependencies
groq>=0.4.0
